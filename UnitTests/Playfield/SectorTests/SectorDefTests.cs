using NUnit.Framework;

namespace UnitTests.Playfield.SectorTests
{
    [TestFixture]
    public class SectorDefTests
    {
        ////Creating a new sectorDef

        //if (location.Coordinate.X < Constants.SECTOR_MIN)
        //{
        //    throw new GameConfigException("Error setting up Coordinate.  Coordinate x < " + Constants.SECTOR_MIN.ToString());
        //}

        //if (location.Coordinate.X > Constants.SECTOR_MAX)
        //{
        //    throw new GameConfigException("Error setting up Coordinate.  Coordinate x > " + Constants.SECTOR_MAX.ToString());
        //}

        //if (location.Coordinate.Y < Constants.SECTOR_MIN)
        //{
        //    throw new GameConfigException("Error setting up Coordinate.  Coordinate y < " + Constants.SECTOR_MIN.ToString());
        //}

        //if (location.Coordinate.Y > Constants.SECTOR_MAX)
        //{
        //    throw new GameConfigException("Error setting up Coordinate.  Coordinate y > " + Constants.SECTOR_MAX.ToString());
        //}

        //TODO: Test that enforces a single starbase per Sector
    }
}
